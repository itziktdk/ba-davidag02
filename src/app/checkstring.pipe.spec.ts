import { CheckstringPipe } from './checkstring.pipe';

describe('CheckstringPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckstringPipe();
    expect(pipe).toBeTruthy();
  });
});

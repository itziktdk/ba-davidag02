import { SortTimeFormatPipe } from './sort-time-format.pipe';

describe('SortTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
